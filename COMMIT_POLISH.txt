Set Industry Packs back to $9.99 and polish the site layout

Pricing: the Pro tier returns to $9.99/month per user across the price
constant, dashboard and console UI, pricing docs, the tests that pin it, and
the website pricing/FAQ/domains copy. Core stays free forever, bring your own
key; checkout remains not-yet-live.

Layout and buttons (each verified with full-page renders at 1440px and 390px):
- Resilience: if the GSAP CDN is blocked (corporate networks, script blockers)
  every .gs element stayed at opacity:0 and the page rendered blank below the
  nav. script.js now un-hides all content when the animation library is absent,
  and index.html gains a matching <noscript> fallback.
- Hero: the download column is now a real column — equal-width stacked
  "Download for macOS / Windows" buttons (proper case, exempt from the .btn
  lowercase transform), centered reassurance line, aligned browser CTA, and the
  terminal fallback in a tidy bordered block. The H1 is scaled so "Get the full
  score." no longer wraps mid-phrase at desktop widths.
- Desktop Studio: the download buttons had been flex children of the intro row
  and stretched into giant blocks; they move to their own full-width row
  between the intro and the terminal, natural height.
- Cards: .pricing-card is a flex column and CTAs pin to the bottom, so the
  install and pricing card buttons align across cards.
- FAQ: two entries both asked "Do I need an account?" — the second becomes
  "Do Industry Packs require an account?".

12 website/brand/pricing test files (75 tests) pass.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01DE8R54MJDNSQ5E6DNmLPMe
