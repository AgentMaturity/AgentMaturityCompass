---
"agent-maturity-compass": minor
---

Add a metadata-only PromptLayer provider/prompt-version drift wrapper across Score, Watch, and Shield APIs, with fail-closed evidence checks for provider versions, canary result hashes, drift-statistic hashes, and alert/waiver hashes.
