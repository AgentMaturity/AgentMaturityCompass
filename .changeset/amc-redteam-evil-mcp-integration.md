---
"agent-maturity-compass": patch
---

Expose built-in Evil MCP agent-provider scenarios through `amc redteam run --evil-mcp`, add `--mcp-attacks` category selection with common aliases, embed linked MCP JSON/Markdown evidence paths in the primary red-team report, and fix MCP provider category filtering for `tool-poisoning`.
