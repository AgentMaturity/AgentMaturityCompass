---
"agent-maturity-compass": patch
---

Document `amc compare --badge` in public onboarding docs and make the two-run comparison path write the comparison SVG badge instead of silently ignoring the flag.
