---
"agent-maturity-compass": patch
---

Expose supply-chain and logistics domain aliases in the domain registry, CLI help/listing, persona docs, and sector-pack docs so operations users can route `supply-chain` to `environment` and `logistics`/`freight`/`3pl`/`warehouse` to `mobility`.
