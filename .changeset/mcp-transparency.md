---
"agent-maturity-compass": minor
---

Add MCP Server (`amc mcp serve`) for IDE integration and Agent Transparency Report (`amc transparency report`) for agent SBOM generation. Includes 14 MCP tools exposing scoring, assurance, fleet, and guide capabilities over JSON-RPC stdio transport.
