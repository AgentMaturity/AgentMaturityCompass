---
"agent-maturity-compass": patch
---

Add agent-scoped diagnostic run aliases. Users can now name a run with `amc run-alias set <alias> <runId|prefix|latest>`, resolve reports with `amc report <alias>`, and see saved aliases in `amc history`.
